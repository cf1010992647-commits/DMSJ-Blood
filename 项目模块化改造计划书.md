# 项目模块化改造计划书

## 一、计划目标

### 1.1 总目标
把当前单体 WPF 上位机，重构成**多工程、多 DLL、清晰依赖方向**架构。

目标结果：

- `BloodAlcohol.Shell`  
  主程序壳。负责启动、主窗口、模块装配、资源合并。
- `BloodAlcohol.Core`  
  共享模型、接口、枚举、基础抽象。
- `BloodAlcohol.Communication`  
  串口、TCP、协议、设备连接、轮询、通信状态。
- `BloodAlcohol.Application`  
  流程引擎、温控业务、配置读取业务、应用级服务。
- `BloodAlcohol.Module.Home`  
  首页 UI + VM + 首页业务编排。
- `BloodAlcohol.Module.Debug`  
  调试页 UI + VM + 调试功能。
- `BloodAlcohol.Module.Communication`  
  通讯配置页 UI + VM。

如果后面需要，还可继续拆：

- `BloodAlcohol.Infrastructure`
- `BloodAlcohol.Logging`
- `BloodAlcohol.Configuration`

## 二、当前项目现状判断

### 2.1 当前本质
当前项目不是“模块化项目”，而是：

**单 WPF 工程 + 文件夹分区 + 部分 MVVM + 多处全局静态耦合**

证据：

### 单主工程
- `Blood Alcohol.sln:5-8`
- `DMSJ_Blood Alcohol.csproj:2-46`

### 主窗口直接引用页面
- `MainWindow.xaml:19-32`

说明：
- 首页、通讯页、设置页都静态挂在主窗口
- 不是模块装配
- 不是动态注册

### 页面直接 new ViewModel
- `Views/HomeView.xaml:5-7`
- `Views/DebugView.xaml:10-12`
- `Views/CommunicationDebugView.xaml:7-9`

说明：
- View 和具体 VM 强绑定
- 不利于模块边界收敛
- 不利于依赖注入和后续 DLL 独立演化

### 全局静态通信中心
- `Services/CommunicationManager.cs:12-26`
- `App.xaml.cs:23-29`
- `HomeViewModel.cs:89-90`
- `CommunicationViewModel.cs:53-57`
- `AxisDebugViewModel.cs:36-37`
- `WorkflowEngine.cs:124-126`

说明：`CommunicationManager` 现在承担太多责任：
- 设备单例
- 配置加载/保存
- 日志总线
- 状态事件
- 自动连接
- PLC polling 生命周期
- TCP 设备映射

它是当前模块化最大阻塞点。

### 服务层反向依赖视图层
- `Services/ISystemInitializationDialogService.cs:1-2, 32-50`
- `Services/IDialogService.cs:10-23`

说明：
- Service 直接 new WPF 对话框
- 非 UI 层被 UI 污染
- 后面拆 DLL 时边界会乱

### VM 中存在大量 WPF 运行时依赖
例如：
- `DebugViewModel.cs:126-135`
- `CommunicationViewModel.cs:197-210`
- `CommunicationViewModel.cs:255-259`
- `AxisDebugViewModel.cs:33-34`
- `PointMonitorViewModel.cs:98`
- `FaultDebugViewModel.cs`、`CoordinateDebugViewModel.cs` 类似

说明：
- 这些 VM 不是纯应用层对象
- 更适合放在“WPF 模块 DLL”
- 不适合直接下沉到纯业务库

## 三、模块化可行性结论

### 3.1 总体结论
**可行。并且值得做。**

### 3.2 可行性评级

| 模块 | 可行性 | 原因 |
|---|---:|---|
| 通讯 DLL | 高 | 已有 `Communication/`、`Protocols/`、通信配置模型，天然边界最清楚 |
| 调试 DLL | 高 | 调试页本身已是一个大容器，子页清晰 |
| 首页 DLL | 中高 | 能拆，但首页现在是业务汇聚中心，依赖最多 |
| 应用/流程 DLL | 中高 | `WorkflowEngine`、温控、配置逻辑有独立价值 |
| 真正插件化模块 | 中 | 需要额外做模块注册/导航/装配，不是第一步必要目标 |

### 3.3 最终判断
可以做到：
- 通讯一个 DLL
- 首页一个 DLL
- 调试一个 DLL

但不能直接“按文件夹搬工程”结束。  
必须先做**边界清洗**。

## 四、建议最终架构

### 4.1 目标工程结构

```text
BloodAlcohol.Shell                 // 主程序壳（WPF EXE）
BloodAlcohol.Core                  // 共享契约、模型、抽象
BloodAlcohol.Communication         // 通讯实现
BloodAlcohol.Application           // 流程/应用业务
BloodAlcohol.Module.Home           // 首页模块（WPF）
BloodAlcohol.Module.Debug          // 调试模块（WPF）
BloodAlcohol.Module.Communication  // 通讯配置模块（WPF）
```

可选后续扩展：

```text
BloodAlcohol.Infrastructure
BloodAlcohol.Logging
BloodAlcohol.Configuration
BloodAlcohol.Tests
```

### 4.2 每层职责

#### A. `BloodAlcohol.Shell`
职责：
- `App.xaml`
- `MainWindow`
- 顶层 Tab / Navigation
- 程序启动
- 生命周期管理
- 模块装配
- 全局异常处理
- 资源字典合并

不负责：
- 具体业务流程
- PLC 读写
- 页面逻辑细节

#### B. `BloodAlcohol.Core`
职责：
- 通用模型
- 枚举
- DTO
- 基础接口
- 抽象服务契约
- 共享事件模型

建议放入：
- `OperationMode`
- 统一日志级别
- 基础结果类型
- 通讯状态模型
- 配置校验结果
- Dispatcher 抽象
- Dialog 抽象
- 模块接口

不放：
- WPF `Brush`
- `Application.Current`
- 具体窗口类

#### C. `BloodAlcohol.Communication`
职责：
- 串口
- TCP
- PLC
- 协议服务
- 通信配置
- 轮询
- 会话映射
- 通信状态发布

建议放入：
- `Communication/Serial/*`
- `Communication/Tcp/*`
- `Protocols/*`
- `PlcPollingService`
- 通信配置读取相关
- 连接协调器
- 设备注册表

注意：不要把当前 `CommunicationManager` 原样搬进去。  
应该拆成更清晰对象。

#### D. `BloodAlcohol.Application`
职责：
- `WorkflowEngine`
- 温控业务
- 流程状态机
- 配置驱动业务
- 首页/调试公用业务规则
- 日志转业务事件

建议放入：
- `WorkflowEngine`
- `TemperatureService`
- 流程快照
- 参数配置校验
- 业务级 orchestration

注意：这里应该依赖通讯抽象，不直接知道具体 WPF 页面。

#### E. `BloodAlcohol.Module.Home`
职责：
- `HomeView`
- `HomeViewModel`
- 首页相关 Presenter / Coordinator / Items / Presentation
- 首页 UI 资源
- 首页交互逻辑

说明：首页模块本质是：
**WPF UI + 首页业务编排层**

建议保留在本模块内：
- WPF `Brush`
- 绑定属性
- 命令
- 首页特有展示转换逻辑

#### F. `BloodAlcohol.Module.Debug`
职责：
- `DebugView`
- 调试总 VM
- 轴调试、故障调试、坐标调试、点位监控等页面
- 调试资源和调试风格

说明：调试模块很适合独立 DLL。  
因为它本身是“功能集合页”。

#### G. `BloodAlcohol.Module.Communication`
职责：
- `CommunicationDebugView`
- `CommunicationViewModel`
- 通讯配置、联机测试 UI

说明：这个模块 UI 性很强。  
建议独立成单独 WPF 模块 DLL，而不是放在通讯实现 DLL 里。

## 五、建议依赖方向

正确依赖方向应该是：

```text
Shell
 ├─> Module.Home
 ├─> Module.Debug
 ├─> Module.Communication
 ├─> Application
 ├─> Communication
 └─> Core

Module.Home
 ├─> Application
 ├─> Communication (最好经由抽象)
 └─> Core

Module.Debug
 ├─> Communication
 └─> Core

Module.Communication
 ├─> Communication
 └─> Core

Application
 ├─> Communication (最好经由抽象)
 └─> Core

Communication
 └─> Core

Core
 └─> 无下游依赖
```

关键规则：
1. `Core` 不依赖任何 WPF 具体 UI
2. `Communication` 不依赖 `Views`
3. `Application` 不依赖 `Views`
4. `Shell` 只装配，不吞业务

## 六、现有代码映射建议

### 6.1 现在可以归到 `Communication` 的内容
目录：
- `Communication/Serial/*`
- `Communication/Tcp/*`
- `Protocols/*`

服务：
- `PlcPollingService.cs`
- 通讯配置相关存储逻辑
- 设备连接协调逻辑

模型：
- `Models/CommunicationSettings.cs`
- `Models/TcpDeviceMapping.cs`

但注意：`CommunicationManager.cs` 不建议整体照搬。  
应拆成多个职责类。

### 6.2 现在可以归到 `Application` 的内容
- `Services/WorkflowEngine.cs`
- `Services/TemperatureService.cs`
- `Services/WorkflowRuntimeSnapshot.cs`
- 一部分配置校验逻辑
- 一部分应用编排逻辑

但前提：先切断对 `CommunicationManager` 静态入口的强绑定。

### 6.3 现在可以归到 `Module.Home` 的内容
- `Views/HomeView.xaml`
- `ViewModels/Home/HomeViewModel.cs`
- `ViewModels/Home/**/*`

但有问题：`HomeViewModel` 很重，依赖：
- `WorkflowEngine`
- `TemperatureService`
- `CommunicationManager`
- `ISystemInitializationDialogService`
- WPF UI dispatcher
- 多个协调器

所以 Home 模块能拆，但不是第一刀。

### 6.4 现在可以归到 `Module.Debug` 的内容
视图：
- `Views/DebugView.xaml`
- `Views/AxisDebugView.xaml`
- `Views/FaultDebugView.xaml`
- `Views/CoordinateDebugView.xaml`
- `Views/PointMonitorView.xaml`
- `Views/WeightToZDebugView.xaml`
- `Views/ParameterConfigView.xaml`
- `Views/CylinderControlView.xaml`
- `Views/PointPositionControlView.xaml`

VM：
- `DebugViewModel.cs`
- `AxisDebugViewModel.cs`
- `FaultDebugViewModel.cs`
- `CoordinateDebugViewModel.cs`
- `PointMonitorViewModel.cs`
- `WeightToZDebugViewModel.cs`
- `ParameterConfigViewModel.cs`
- `CylinderControlViewModel.cs`
- `PointPositionControlViewModel.cs`
- `AxisAddressConfigViewModel.cs`

模型：
- `AxisDebugAddressConfig.cs`
- 调试相关配置模型

### 6.5 现在可以归到 `Module.Communication` 的内容
- `Views/CommunicationDebugView.xaml`
- `CommunicationViewModel.cs`
- 通讯页展示模型 `LogItem`
- 通讯页相关样式资源

### 6.6 现在可以归到 `Shell` 的内容
- `App.xaml`
- `App.xaml.cs`
- `MainWindow.xaml`
- `MainWindow.xaml.cs`

但：`App.xaml.cs` 里现在有大量业务控制逻辑：
- 加载通讯设置
- 自动连接
- 页面 VM 停止
- PLC 异常处理
- 配置校验

这些后面要逐步下沉，不然壳层会过厚。

## 七、核心阻塞点与解决策略

### 7.1 阻塞点一：`CommunicationManager` 全局静态中心

#### 当前问题
`CommunicationManager` 现在既像：
- service locator
- device registry
- event hub
- config store
- bootstrapper
- app state center

结果：
- 首页依赖它
- 调试依赖它
- 通讯页依赖它
- App 启动/关闭依赖它
- WorkflowEngine 依赖它

#### 风险
如果直接拆 DLL：
- 所有 DLL 都会继续引用它
- 表面上变多工程
- 实际仍然中心化耦合

#### 解决策略
把它拆成几类职责：

1. `ICommunicationRuntime`
   - 当前通信状态
   - 连接/断开入口
   - 事件

2. `IPlcAccessService`
   - PLC 读写
   - 锁控制
   - 地址级访问

3. `ICommunicationConfigService`
   - 加载/保存通信配置
   - 配置校验

4. `ICommunicationLogSource`
   - 通讯日志流

5. `ITcpDeviceSessionService`
   - 设备连接映射
   - IsDeviceConnected
   - DeviceKey 解析

这样后：
- 首页只拿它需要那部分
- 调试页只拿 PLC/通信访问能力
- 通讯页只拿配置和连接能力

### 7.2 阻塞点二：服务层直接依赖视图层

#### 当前问题
- `ISystemInitializationDialogService.cs` 直接 `using Blood_Alcohol.Views`
- `FolderDialogService` 直接调用 `OpenFolderDialog`

#### 风险
- 非 UI DLL 不能独立存在
- Application 层带 WPF 壳污染

#### 解决策略
把“接口”和“实现”拆开：
- 接口放 `Core`
- WPF 实现放 `Shell` 或对应 UI 模块

例如：
- `IDialogService`
- `ISystemInitializationDialogService`

接口只描述行为，不引用窗口类。  
具体 `AxisSafePositionConfirmDialog` 实现留在 WPF 模块里。

### 7.3 阻塞点三：ViewModel 里混入 WPF 线程对象

#### 当前问题
典型代码：
- `Application.Current.Dispatcher.Invoke`
- `Dispatcher.CurrentDispatcher`
- `Brushes.*`

#### 风险
- VM 只能留在 WPF 层
- 无法提纯业务层
- 单元测试不友好

#### 解决策略
分两类看：

##### A. UI 绑定专属内容
如 `Brush`、按钮文案、状态色。  
可以保留在模块 VM 中，不强求纯净。

##### B. 线程调度
统一经由 `IUiDispatcher`。  
项目里已经有雏形：
- `Services/IUiDispatcher.cs`
- `HomeViewModel.cs:63`
- `HomeViewModel.cs:687, 700+`

这说明项目已经朝这个方向走了一半。  
后面应该推广到：
- Debug
- Communication
- 其它监控 VM

### 7.4 阻塞点四：主窗口写死页面

#### 当前问题
`MainWindow.xaml:19-32` 直接写死：
- `HomeView`
- `CommunicationDebugView`
- `DebugView`

#### 风险
- 壳层必须知道所有页面类型
- 模块 DLL 仍是强引用
- 后续不可扩展

#### 解决策略
分两阶段：

##### 第一阶段
仍允许壳层静态引用模块页面，只要页面类已经从主工程移到模块 DLL。  
这是**低风险落地方案**。

##### 第二阶段
再改成：
- 模块注册
- 页面工厂
- 导航项注册
- ViewModel / View 定位

这样才接近真正“模块装配”。

### 7.5 阻塞点五：首页模块太重

#### 当前问题
`HomeViewModel` 是业务汇聚中心：
- 流程
- 日志
- PLC
- 温控
- 批次
- 模式切换
- 后台任务
- 轨迹事件
- 配置读取

参考：
- `HomeViewModel.cs:61-111`
- `HomeViewModel.cs:687-718`
- `HomeViewModel.cs:793-794`
- `HomeViewModel.cs:1510-1513`

#### 风险
首页模块一拆，最容易牵扯全局。

#### 解决策略
先不急着拆首页。  
先抽：
- 通讯能力接口
- 应用层流程接口
- 对话框抽象
- 日志输入接口

等边界清掉，再搬 Home。

## 八、推荐实施路线图

下面给出实际可执行顺序。  
按工业项目风险控制，建议分 6 个阶段。

### 阶段 0：准备阶段
目标：确认边界，避免拆到一半返工。

#### 要做
1. 画依赖图
2. 标记全局静态入口
3. 标记 UI -> Service、Service -> View 反向依赖
4. 列出公共模型归属
5. 确定最终工程命名

#### 输出
- 模块边界表
- 类迁移清单
- 依赖收敛清单

#### 风险
低

### 阶段 1：先抽共享契约 `Core`
目标：给后面拆 DLL 准备公共地基。

#### 要做
1. 建 `Core` 工程
2. 迁移纯共享内容：
   - 枚举
   - DTO
   - 结果模型
   - 基础接口
3. 抽 UI 相关接口：
   - `IUiDispatcher`
   - `IDialogService`
   - `ISystemInitializationDialogService`
4. 抽通信抽象接口：
   - 通信状态接口
   - PLC 访问接口
   - 日志事件接口

#### 为什么先做
因为后面所有 DLL 都要引用 `Core`。  
不先抽，后面会循环依赖。

#### 风险
中低

#### 成功标志
- `Core` 不依赖 WPF 页面
- 其它工程开始依赖 `Core`

### 阶段 2：拆通讯实现 `Communication`
目标：先拿下最适合拆的部分。

#### 要做
1. 新建 `Communication` 工程
2. 迁移：
   - `Communication/Serial/*`
   - `Communication/Tcp/*`
   - `Protocols/*`
   - `PlcPollingService`
3. 拆分 `CommunicationManager`
4. 用组合对象替代单静态中心
5. 配置模型和连接状态模型归位

#### 建议结果
不要保留“超级静态类”。  
改成“运行时对象 + 接口”。

#### 风险
中

#### 最大收益
- 先建立真正 DLL 边界
- Debug/Home 后续都能依赖它

### 阶段 3：拆应用层 `Application`
目标：把流程和业务服务从 WPF 壳周边拿开。

#### 要做
1. 新建 `Application` 工程
2. 迁移：
   - `WorkflowEngine`
   - `TemperatureService`
   - `WorkflowRuntimeSnapshot`
   - 业务级配置校验
3. 改为依赖通讯抽象接口，而不是静态 `CommunicationManager`
4. 统一业务日志模型

#### 风险
中高

#### 注意
这里最容易暴露隐藏耦合。  
尤其 `WorkflowEngine.cs:114-126` 这种直接 new 协议服务 + 直接拿全局锁。

### 阶段 4：拆通讯 UI 模块 `Module.Communication`
目标：通讯配置页独立 DLL。

#### 要做
1. 新建 `Module.Communication`
2. 迁移：
   - `CommunicationDebugView.xaml`
   - `CommunicationViewModel.cs`
3. 让 VM 通过接口访问通信运行时
4. 把 WPF 样式和资源跟页一起带走

#### 风险
中低

#### 理由
这是 UI 层里最适合最先拆的模块之一。

### 阶段 5：拆调试模块 `Module.Debug`
目标：调试体系独立 DLL。

#### 要做
1. 新建 `Module.Debug`
2. 迁移调试相关视图和 VM
3. 调试功能通过接口访问 PLC / 通信 / 配置
4. 把调试相关模型迁移归位

#### 风险
中

#### 注意
像 `AxisDebugViewModel` 这种直接依赖：
- `CommunicationManager.PlcAccessLock`
- `ConfigService`

需要改成通过接口拿。

### 阶段 6：拆首页模块 `Module.Home`
目标：首页独立 DLL。

#### 要做
1. 新建 `Module.Home`
2. 迁移：
   - `HomeView`
   - `HomeViewModel`
   - `ViewModels/Home/**/*`
3. 让首页依赖：
   - `Application`
   - `Communication` 抽象
   - `Core`
4. 把首页中对 `CommunicationManager` 的调用收口到 adapter/service

#### 风险
高

#### 原因
首页是当前最大业务汇聚点。  
建议最后拆。

### 阶段 7：壳层瘦身 `Shell`
目标：让主程序只做装配。

#### 要做
1. `MainWindow` 不再直接承载业务
2. `App.xaml.cs` 中业务关闭逻辑下沉
3. 异常处理/自动连接/配置验证收敛到应用启动协调器
4. 资源字典整理为模块级或全局级

#### 风险
中

## 九、每阶段风险评估

| 阶段 | 风险 | 说明 |
|---|---:|---|
| 阶段 0 准备 | 低 | 纯分析整理 |
| 阶段 1 Core | 中低 | 主要是接口归位 |
| 阶段 2 Communication | 中 | 会动通信主干 |
| 阶段 3 Application | 中高 | 流程/温控/配置会暴露隐藏依赖 |
| 阶段 4 Module.Communication | 中低 | UI 模块边界较清晰 |
| 阶段 5 Module.Debug | 中 | 调试 VM 与通信绑得较深 |
| 阶段 6 Module.Home | 高 | 首页业务最复杂 |
| 阶段 7 Shell 瘦身 | 中 | 需保证启动与关闭行为不变 |

## 十、建议优先级

### 最推荐顺序
1. Core
2. Communication
3. Module.Communication
4. Application
5. Module.Debug
6. Module.Home
7. Shell 收口

### 为什么不是先拆首页
因为首页耦合最深。  
先拆首页，等于先拆最难部分，容易让项目中途失控。

## 十一、验证标准

模块化不是“工程变多”就算成功。  
应有明确验收标准。

### 11.1 结构验收
- 主工程不再承载全部 ViewModel
- 通讯实现不再依赖 `Views`
- `Core` 不依赖 WPF 具体页面
- `Application` 不直接 new 页面

### 11.2 依赖验收
- `Shell -> Module` 单向
- `Module -> Core/Application/Communication`
- 无反向依赖
- 无循环引用

### 11.3 行为验收
- 启动流程不变
- 自动连接不变
- 首页日志/流程不变
- 调试页 PLC 读写不变
- 关闭时资源释放顺序不变

### 11.4 工业现场验收
重点验证：
- PLC 通讯稳定性
- TCP 设备连接稳定性
- 自动/手动切换
- 流程启动停止
- 温控读写
- 异常弹窗
- 日志完整性
- 程序退出释放

## 十二、不建议做法

### 12.1 不建议直接按文件夹搬工程
坏处：
- 依赖还是原样
- 只是“物理分拆”，不是“架构分拆”

### 12.2 不建议一步上插件化
坏处：
- 复杂度过高
- 工业项目回归成本高
- 当前收益不大

### 12.3 不建议先碰首页
坏处：
- 首页最核心
- 风险最高
- 容易连带流程、日志、温控、模式切换全部受影响

### 12.4 不建议保留超级静态 `CommunicationManager`
坏处：
- 模块边界永远脏
- 后期测试和维护成本高

## 十三、推荐最终落地版本

对这个项目，建议目标不要一步追求“插件化平台”。  
更实用目标是：

### 目标版本 V1
实现：
- 多工程
- 多 DLL
- 清晰依赖方向
- 壳层装配
- 通讯 / 调试 / 首页独立模块

这已经很有价值。

### 目标版本 V2
再考虑：
- 模块注册
- 动态导航
- 功能开关
- 按模块测试
- 更细粒度替换实现

## 十四、最终结论

**这个项目可以做模块化，且适合做。**  
最合理目标不是一步做成插件系统，而是先做：

1. `Core` 共享契约  
2. `Communication` 通讯实现 DLL  
3. `Module.Communication` 通讯页 DLL  
4. `Application` 流程/温控业务 DLL  
5. `Module.Debug` 调试页 DLL  
6. `Module.Home` 首页 DLL  
7. `Shell` 主程序壳装配

最大障碍不是 WPF，也不是 DLL 数量。  
最大障碍是当前三类耦合：

- `CommunicationManager` 全局静态中心
- 服务层反向依赖 View
- 首页/调试 VM 直接吃 WPF 运行时和通信实现

这三处清掉后，模块化能落地，而且后续维护会明显轻松。
